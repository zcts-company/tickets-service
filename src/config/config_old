export const config = {
    interval: 5, // interval seconds
    nemo: {
        server:{
            port:48659
        }
    },
    travelline: {
        server:{
            port:48658
        }
    }
    
}